export const metadata = {
    title: "Login | Blog Management System",
  };
  
  export default function LoginLayout({ children }) {
    return (
      <html lang="en">
        <body style={{ margin: 0, padding: 0 }}>
          {children}
        </body>
      </html>
    );
  }
  